export PRODUCTION=1
export SQLALCHEMY_DATABASE_URI='' # DATABASE URI (must include username and password, as well as protocol)
export CELERY_BROKER_URL=''              # prolly something like pyamqp://user:pass@location/
export SECRET_KEY=''  # longish random string
export AUTH_KEY=''        # Get this from someone (prolly jack)

gunicorn -w 4 -b 127.0.0.1:5000 scrimmage:app
